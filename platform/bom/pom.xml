<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.acme</groupId>
    <artifactId>backend-template</artifactId>
    <version>${revision}</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>platform-bom</artifactId>
  <packaging>pom</packaging>

  <name>platform-bom</name>
  <description>Version contract for consumers outside this reactor. A service extracted from
    this monorepo imports this BOM and gets a coherent set of platform libraries
    without pinning any of them individually.</description>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.acme</groupId>
        <artifactId>kernel</artifactId>
        <version>${revision}</version>
      </dependency>
      <dependency>
        <groupId>com.acme</groupId>
        <artifactId>arch-test</artifactId>
        <version>${revision}</version>
      </dependency>
      <dependency>
        <groupId>com.acme</groupId>
        <artifactId>web-support</artifactId>
        <version>${revision}</version>
      </dependency>
      <dependency>
        <groupId>com.acme</groupId>
        <artifactId>persistence-support</artifactId>
        <version>${revision}</version>
      </dependency>
      <dependency>
        <groupId>com.acme</groupId>
        <artifactId>messaging-support</artifactId>
        <version>${revision}</version>
      </dependency>
      <dependency>
        <groupId>com.acme</groupId>
        <artifactId>observability-support</artifactId>
        <version>${revision}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
</project>
