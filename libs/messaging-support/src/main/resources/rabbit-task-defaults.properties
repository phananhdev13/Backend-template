# A task queue is worthless without retry: every failure path a broker has redelivers, and
# without this a first failure goes straight to reject-and-drop. A service's own
# application.yml can still turn it off or retune it - this is a default, not a lock.
spring.rabbitmq.listener.simple.retry.enabled=true
